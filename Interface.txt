Listener
	# virtual void receive(string message) = 0;
	
Wireless
	+ bool send(string address, string message) const;
	+ bool start();
	+ bool stop(); 
	+ void registerListener(Listener &listener);
	+ void unregisterListener();
	- void beacon() const;
	- bool init();
	- Listener listener;
	
Neighbor
	+ string getAddress() const;
	+ void setAddress(string address);
	+ bool getArrived() const;
	+ void setArrived(bool arrived);
	+ int getAge() const;
	+ void setAge(int age);
	- string address;
	- bool arrived;
	- int age;
	
NDP: Listener
	+ bool sendMessage(string address, sting message) const;
	+ void registerMessageListener(Listener &listener);
	+ void unregisterMessageListener();
	+ void registerTableChangeListener(Listener &listener);
	+ void unregisterTableChangeListener();
	+ Structure getNeighbors() const;
	# void receive(string message);
	- void update();
	- Wireless wireless
	- Listener messageListener;
	- Listener tableChangeListener;
	- Sructure<Neighbor> neighbors;
	

	
	
	
	